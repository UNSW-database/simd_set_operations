[algorithm_set.scalar_linear]
twoset = [
    "zipper_asm",
    "zipper_branch_optimized_asm",
    "zipper_branchless_asm",
    "zipper_loop_optimized_asm",
    "zipper_noindex_asm",
    "zipper_bad_branch_asm",
    "zipper_ref",
#   "zipper_branch_optimized_ref",
#   "zipper_loop_optimized_ref"
]
#twoset_to_kset = ["svs"]

# [algorithm_set.scalar_search]
# fsearch = ["binary", "exponential", "golomb", "interpolation", "extrapolation_lookback", "extrapolation_lookahead"]
# fsearch_to_twoset = ["basic"]
# twoset_to_kset = ["svs"]
# fsearch_to_kset = ["sequential", "rsequential", "small_adaptive", "max"]

[experiment.example]
count_only = false
repeats_per_databin = 10
runs_per_trial = 10
algorithm_sets = ["scalar_linear"]
